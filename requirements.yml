---
# Collections used by the roles. Install with:
#   ansible-galaxy collection install -r requirements.yml -p .collections

collections:
  - name: community.general
    version: 9.5.0
  - name: ansible.posix
    version: 1.6.2
  - name: community.docker
    version: 4.1.0
